set $mod Mod4
set $alt Mod1

font pango:Fira Mono 12

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Shift+Return exec google-chrome --remote-debugging-port=9222

# kill focused window
bindsym $mod+w kill
bindsym $mod+Shift+w exec --no-startup-id $HOME/.dotfiles/i3/shutdown

bindsym $mod+Shift+space exec --no-startup-id rofi -show run
bindsym $mod+space exec --no-startup-id rofi -modi drun -show drun -show-icons

bindsym $mod+Tab exec --no-startup-id rofi -show window -show-icons -filter "-Google-chrome "

bindsym $mod+p exec --no-startup-id rofi -modi pass:$HOME/.dotfiles/rofi/rofi-pass -show pass
bindsym $mod+b exec --no-startup-id rofi -modi locate:$HOME/.dotfiles/rofi/rofi-locate -show locate -matching fuzzy -sort -no-levenshtein-sort -columns 1

bindsym $mod+e exec --no-startup-id dbus-send --session --print-reply --dest=org.freedesktop.Notifications /org/freedesktop/Notifications org.freedesktop.Notifications.ShowNotifications

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# scratchpad
bindsym $mod+m move scratchpad
bindsym $mod+Shift+m scratchpad show

# split in horizontal orientation
bindsym $mod+bar split h

# split in vertical orientation
bindsym $mod+minus split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+bracketright layout toggle splitv splith tabbed stacking

# toggle tiling / floating
bindsym $mod+bracketleft floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+bracketleft focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# make window sticky
bindsym $mod+s sticky toggle

bindsym Ctrl+$alt+Delete --release exec "xset dpms force off"

# screenshot
bindsym Print --release exec --no-startup-id import -silent png:- | xclip -i -selection clipboard -target image/png
bindsym shift+Print --release exec --no-startup-id import -silent ~/Screenshot-$(date +%Y%m%d_%H%M%S).png

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
# switch to workspace
bindsym $mod+n exec --no-startup-id exec $HOME/.dotfiles/rofi/rofi-rename-workspace

bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10:mail

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1; workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2; workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3; workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4; workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5; workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6; workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7; workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8; workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9; workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10; workspace number 10

# move between monitors
bindsym $mod+Up move workspace to output up
bindsym $mod+Down move workspace to output down
bindsym $mod+Left move workspace to output left
bindsym $mod+Right move workspace to output right

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "topleft" {
  bindsym h     resize grow   left 5 px or 5 ppt
  bindsym l     resize shrink left 5 px or 5 ppt
  bindsym Left  resize grow   left 5 px or 5 ppt
  bindsym Right resize shrink left 5 px or 5 ppt

  bindsym k    resize grow   up 5 px or 5 ppt
  bindsym j    resize shrink up 5 px or 5 ppt
  bindsym Up   resize grow   up 5 px or 5 ppt
  bindsym Down resize shrink up 5 px or 5 ppt

  bindsym space  mode "bottomright"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}

mode "bottomright" {
  bindsym l     resize grow   right 5 px or 5 ppt
  bindsym h     resize shrink right 5 px or 5 ppt
  bindsym Right resize grow   right 5 px or 5 ppt
  bindsym Left  resize shrink right 5 px or 5 ppt

  bindsym j    resize grow   down 5 px or 5 ppt
  bindsym k    resize shrink down 5 px or 5 ppt
  bindsym Down resize grow   down 5 px or 5 ppt
  bindsym Up   resize shrink down 5 px or 5 ppt

  bindsym space  mode "topleft"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}

bindsym $mod+r mode "topleft"

mode "move" {
    bindsym Tab focus right

    bindsym Left  move left
    bindsym Down  move down
    bindsym Up    move up
    bindsym Right move right

    bindsym h     move left
    bindsym j     move down
    bindsym k     move up
    bindsym l     move right

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+t mode "default"
}
bindsym $mod+t mode "move" focus floating

bindsym $mod+plus bar mode toggle
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    height 48
    position top
    i3bar_command i3bar -t
    status_command $HOME/.local/bin/py3status -c ~/.config/i3/py3status.config  #-b

    tray_output primary
    tray_padding 3
    colors {
        background #2C394Ca0
        statusline #ffeedd
        separator #666666

        focused_workspace  #4c7899ff  #285577ff #ffffffff
        active_workspace   #333333ff  #5f676aff #ffffffff
        inactive_workspace #333333ff  #222222ff #888888ff
        urgent_workspace   #2C394Ca0  #2C394Ca0 #ff9f9fff
        binding_mode       #2f343aff  #900000ff #ffffffff
    }
}

new_window 1pixel
for_window [class="Yad"] floating enable
for_window [class="gnome-control-center"] floating enable
for_window [class="Gnome-tweaks"] floating enable
for_window [class="gnome-calendar"] floating enable
for_window [class="Clocks"] floating enable
for_window [instance="calendar.google.com"] floating enable
for_window [class="Nautilus"] floating enable
for_window [class="Dconf-editor"] floating enable
for_window [class="vlc"] floating enable
for_window [class="gnuplot.*"] floating enable
for_window [class="^.*"] border pixel 3
for_window [instance="calendar.google.com"] floating enable
for_window [instance="www.google.com__calendar_event"] floating enable
for_window [instance="crx_jgeocpdicgmkeemopbanhokmhcgcflmi"] floating enable
for_window [instance="crx_jgeocpdicgmkeemopbanhokmhcgcflmi"] resize set 600 800
for_window [instance="web.whatsapp.com"] resize set 635 800


client.unfocused        #4c4c4c   #4c4c4c   #4c4c4c   #4c4c4c
client.focused          #CC0000   #CC0000   #CC0000   #3eaeff
client.focused_inactive #CC000066 #CC000066 #CC000066 #3eaeff66
client.urgent           #00FF00   #00FF00   #00ff00   #00FF00

gaps inner 7
gaps outer -7
smart_gaps on
smart_borders on

focus_follows_mouse no
workspace_auto_back_and_forth yes

exec --no-startup-id $HOME/.local/bin/i3-notifier
exec --no-startup-id xss-lock -n '/usr/lib/x86_64-linux-gnu/xsecurelock/dimmer' -l -- '/usr/share/goobuntu-desktop-files/xsecurelock.sh'
exec --no-startup-id xautolock -detectsleep -time 10 -locker '/usr/share/goobuntu-desktop-files/xsecurelock.sh'
exec --no-startup-id $HOME/.dotfiles/bin/mute-on-lock.sh
exec --no-startup-id compton
exec --no-startup-id xcape -e 'Caps_Lock=Escape' -t 100
