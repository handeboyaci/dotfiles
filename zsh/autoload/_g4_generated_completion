#autoload
# Autogenerated by //experimental/users/orbekk/GenerateCompletion.hs.
_g4_describe_commands() {
  local -a _g4_commands
  _g4_commands=(
    "add:Open files for add."
    "addbranch:Initialize an access control file for a new branch."
    "annotate:Display each line of a file annotated with the revision number of the latest change to the line."
    "approvals:List approvals for a changelist."
    "approve:Approve a pending changelist for submit."
    "authors:Display a list of authors by percentage of code written."
    "blame:Display last author and change#/revision for each line of code."
    "branch:Create or edit a branch."
    "branches:Display a list of branches."
    "change:Create or edit a change description."
    "changes:List pending or submitted changes."
    "cherrypick:Integrate specific revisions into the current fast branch."
    "citc:Creates or deletes a CitC client."
    "cleanup:Interactively deletes unused clients."
    "client:Create, edit, or display a client specification."
    "clients:Display list of clients."
    "clonebranch:Clone a fast branch."
    "commit:Commit (snapshot) current state of the client."
    "commitlog:List commits for a changelist."
    "copy:Copy files or directories."
    "counters:Display list of server counters and values."
    "delete:Open files for delete."
    "deps:Print depot path inclusions and exclusions needed to build target(s)."
    "describe:Display one or more pending or submitted changes."
    "diff:Compare client file(s) against depot file(s)."
    "diff2:Display diff of two depot files."
    "diffstats:Count added, deleted, and changed lines."
    "dirs:List directories that match a path expression."
    "edit:Open files for edit."
    "filelog:List file revision history."
    "files:List files in depot."
    "findreviewers:Suggest a set of reviewers for a given changelist."
    "fix:Runs automated fixes on code."
    "fstat:Output detailed file information."
    "g4d:\"g4d\" will go to a directory, while \"p4 g4d\" prints the directory."
    "have:Display the revision to which a file in the current client was last synced."
    "help:Display help message."
    "hipupdate:Update HIP metadata."
    "info:Display information about the client and Piper server."
    "integrate:Integrate changes between files."
    "integrated:Display submitted integrations."
    "label:Create, edit, or display a label specification."
    "labels:Display list of labels."
    "lint:Displays style errors detected by 'glint'."
    "mail:Requests a code review from the specified reviewer(s)."
    "makelink:Create the SrcFS READONLY symlink for a fast branch."
    "map:Edit the VERSION_MAP file in a Citc client, or a components enabled Piper client."
    "mapbranch:Maps an existing fast branch."
    "move:Move files or directories."
    "newbranch:Create a new fast branch."
    "nothave:List files present in your client that are not known in Piper."
    "opened:Display list of files opened in pending changelists."
    "owners:List the owners of file paths in the depot."
    "pack:Write a patch for a changelist to stdout."
    "patch:Pack a change and unpack it in the local client."
    "pending:Display pending changelists."
    "populate:Make packages in a fast branch modifiable."
    "presubmit:Executes METADATA Presubmit checks for a changelist."
    "print:Print contents of files in depot."
    "protect:Display SrcProtect permissions."
    "readability:Display Google readability for a user."
    "reopen:Change the type or changelist number of an opened file."
    "resolve:Resolve pending integrates."
    "resolved:Display resolved but unsubmitted integrates."
    "restore:Restore the state of the client to a previous snapshot."
    "revert:Revert open files from a pending changelist."
    "rollback:Undoes the effect of a submitted changelist."
    "set:Display values of Piper configuration variables."
    "sizes:Display sizes of files in depot."
    "speclog:Displays a log of when a spec changed."
    "submit:Submit opened files as a permanent change."
    "switch:Creates or changes the target of a CITC switching client."
    "switches:List the current user's switch clients."
    "sync:Update local files with changes in the depot or client view."
    "tabcomplete:List token completions of a command line."
    "test:Runs tests affected by open files in Blaze."
    "typemap:Display the typemap."
    "unpack:Unpacks a packfile into the current client."
    "upload:Upload a changelist to Critique for review."
    "user:Create, edit, or display a user specification."
    "users:Display list of users."
    "where:Display file mappings for the current client view."
    "whitelist:Edit SrcProtect permissions for Piper Groups."
  )

  _describe -t commands 'Piper commands' _g4_commands
}
_g4_command_completion() {
  local command="$1"
  case "$command" in
    (add)
    _arguments \
      -c'[<change#> Specifies a changelist in which to create the files.]':changelist:_g4_describe_cls \
      -f'[Allows filenames to contain special characters that would otherwise have a special meaning to Piper....]' \
      -n'[Turns on display-only mode. In display-only mode no changes are made to any changelist or file, but ...]' \
      -t'[<filetype> Opens files as type <filetype>. Type "p4 help filetypes" for more information about filet...]' \
      --openers'[Prints other places where files being opened are already open.]' \
      '*:file:_files'

    ;;
    (addbranch)
    _arguments \
      -n'[Dry run. Don'\''t create the permissions file, just validate all command line arguments.]' \
      '*:file:_files'

    ;;
    (annotate)
    _arguments \
      -a'[Displays the range of revisions that contain each line, rather than just the latest revision to touc...]' \
      -c'[Displays the changelist number rather than the revision number.]':changelist:_g4_describe_cls \
      -i'[Follows branches. For example, if a file was created in a branch and then integrated into mainline, ...]' \
      -q'[Suppresses the header info for each file. Without the -q flag, the output for each file begins with ...]' \
      -u'[Shows the author of each line in -ztag mode.]' \
      -w'[Ignores whitespace in files while annotating.]' \
      '*:file:_files'

    ;;
    (approvals)
    _arguments \
      -c'[<change#> Specifies the changelist to list approvals for. The change# can be provided without the -c...]':changelist:_g4_describe_cls \
      -t'[<username>,... Displays missing approvals as if the specified username(s) have also approved the cha...]' \
      '*:file:_files'

    ;;
    (approve)
    _arguments \
      -c'[<change#> Specifies the changelist to approve. The -c can be omitted if the change# is the last argu...]':changelist:_g4_describe_cls \
      --approve_only'[Only grant or undo approval, making no changes to LGTM.]' \
      --lgtm_only'[Only grant or undo LGTM, making no changes to approval.]' \
      --undo'[Undoes an approval/LGTM for a pending changelist, instead of approving.]' \
      '*:file:_files'

    ;;
    (authors)
    _arguments \
      '*:file:_files'

    ;;
    (blame)
    _arguments \
      -a'[Displays the range of revisions that contain each line, rather than just the latest revision to touc...]' \
      -c'[Displays the changelist number rather than the revision number.]':changelist:_g4_describe_cls \
      -i'[Follows branches. For example, if a file was created in a branch and then integrated into mainline, ...]' \
      -q'[Suppresses the header info for each file. Without the -q flag, the output for each file begins with ...]' \
      -w'[Ignores whitespace in files while annotating.]' \
      '*:file:_files'

    ;;
    (branch)
    _arguments \
      -d'[Deletes the branch specification for <branchname>. Does not delete any local files.]' \
      -i'[Creates or updates a branch from a branch specification supplied via standard input.]' \
      -o'[Prints the specification for <branchname> to stdout.]' \
      --revision'[Prints a specific revision of the branch spec instead of the latest revision.]' \
      '*:file:_files'

    ;;
    (branches)
    _arguments \
      -e'[<namepattern> Restricts results to branches that match <namepattern>. The <namepattern> can include ...]' \
      -m'[<max> Restricts results to the first <max> branches.]' \
      -u'[<username> Restricts results to branches owned by <username>.]' \
      '*:file:_files'

    ;;
    (change)
    _arguments \
      -b'[<bugs> Specifies the comma-separated <bugs> that this changelist addresses; sets the BUG= changelist...]' \
      -d'[Deletes the change specification for <change#>. Changelists with open files cannot be deleted, unles...]' \
      --reopen_files'[When deleting a change specification, reopens any files from <change#> in the default changelist.]' \
      --revert_files'[When deleting a change specification, reverts any open files from <change#>.]' \
      -i'[Creates or updates a changelist from a change specification supplied via standard input.]' \
      -m'[<reviewers> Updates the comma-separated <reviewers> that should review this change before submission...]' \
      -o'[Prints the specification for <change#> to stdout. If no <change#> is provided, prints the change spe...]' \
      -r'[When invoked as unchange, reverts all files open in <change#> before deleting it. See also --revert_...]' \
      {--cc,-z}'[<emails> Specifies the comma-separated <emails> that will be cc'\''d on the code review thread; sets th...]' \
      --desc'[<description> Sets the description of the change without invoking the editor. Can be combined with -...]' \
      --set_tag'[<KEY=value> Sets an arbitrary tag in the changelist description. Specifying an empty value (or omitt...]' \
      --tbr'[<reviewers> Specifies the comma-separated <reviewers> that should promptly review this change after ...]' \
      -c'[The affected changelist]':changelist:_g4_describe_cls \
      '*:file:_files'

    ;;
    (changes)
    _arguments \
      -c'[<clientname> Restricts the displayed changes to those in <clientname>.]':changelist:_g4_describe_cls \
      -i'[Displays information about files integrated into files matching filespec.]' \
      -l'[Displays the full description text.]' \
      -L'[Displays up to 250 characters of each change description.]' \
      -m'[<max> Specifies the maximum number of changes to display per filespec.]' \
      -s'[<status> Restricts the displayed changes to those with a status of <status> (pending or submitted).]' \
      -t'[Displays the change'\''s creation time.]' \
      -u'[<username> Restricts the displayed changes to those by <username>.]' \
      --as_set'[Deduplicates changes that would match multiple arguments, instead of returning results for each argu...]' \
      --files'[Displays the files in the changelist.]' \
      '*:file:_files'

    ;;
    (cherrypick)
    _arguments \
      -c'[<change#> Integrate all revisions which are part of a single submitted changelist. If -c is not prov...]':changelist:_g4_describe_cls \
      {-f,--force}'[Proceed even if something looks wrong, e.g. some files cannot be represented on the branch. Without ...]' \
      {-n,--dryrun}'[Simulate by passing -n to '\''p4 integrate'\''.]' \
      {-p,--populate}'[Populate the branch with the files necessary to do the cherrypick, but don'\''t schedule any integratio...]' \
      '*:file:_files'

    ;;
    (citc)
    _arguments \
      -c'[<change#> The created client is initially synced to the given changelist number instead of head.]':changelist:_g4_describe_cls \
      --nocomponents'[The created client has components disabled. To modify existing clients, see '\''p4 help map'\''.]' \
      -d'[Deletes the CitC workspace named <name> and the associated Piper client.]' \
      -f'[Requires -d. Reverts all pending changes and files in the workspace before deleting it.]' \
      {--multichange,--nomultichange}'[Creates a client that is either multichange or not multichange, overriding the user default. To modi...]' \
      -q'[Do not display the first line of the output; this allows one to run the output directly. Ex: type $(...]' \
      '*:citc client:_g4_describe_citc_clients'

    ;;
    (cleanup)
    _arguments \
      --delete_empty'[Deletes all clients which have no open files, pending changes, or nothave files (see "p4 help nothav...]' \
      --no_numbered_changes'[Prompts to delete clients with no pending, numbered changes. Clients with only a default change and/...]' \
      '*:file:_files'

    ;;
    (client)
    _arguments \
      -a'[<path> Modifies the client'\''s View by adding or removing a mapping for the specified path. The path m...]' \
      -b'[<branch> Sets the Branch field of the spec to a branch under //depot/branches/. See the spec for mor...]' \
      -d'[Deletes client <clientname> and removes its client specification from Piper. Does not delete any loc...]' \
      -f'[Requires -d. Reverts all pending changes and files in the client before deleting it.]' \
      -i'[Creates a new client specification from a form supplied via standard input.]' \
      -m'[<components> Most engineers should use '\''-m components'\'' (the default), which will map Components at t...]' \
      -o'[Prints the specification for <clientname> to stdout. If the <clientname> is not provided, prints the...]' \
      -p'[<p4_depot_paths> p4_depot_paths Add the contents of a p4_depot_paths file to the current client. The...]' \
      -r'[<readonly> Determines whether or not to use SrcFS. If '\''-r srcfs'\'' is specified, this will create a tr...]' \
      -t'[<templatename> Specifies an existing client <templatename> to use as a template. The option fields (...]' \
      --deps'[<buildtarget> Add the packages required to build <buildtarget> to the current client. <buildtarget> ...]' \
      --implicit_deps'[<buildtarget> Controls whether the target '\''//tools/p4deps:implicit'\'' is implicitly included in the ta...]' \
      --remove_local_root'[Recursively deletes the directory corresponding to the client'\''s root. This requires that the directo...]' \
      --replace'[Requires -a or -p be specified. Removes existing mappings before adding the specified mappings to th...]' \
      --revision'[<revision#> Prints a specific revision of the client spec instead of the latest revision.]' \
      --set_option'[<option> Sets the specified option in the client spec. For example "g4 client --set_option nomultich...]' \
      '*:file:_files'

    ;;
    (clients)
    _arguments \
      -e'[<namefilter> Restricts the displayed clients to those whose names match <namefilter>. <namefilter> c...]' \
      -m'[<max> Specifies the maximum number of clients to display.]' \
      -u'[<username> Restricts the displayed clients to those belonging to <username>.]' \
      '*:file:_files'

    ;;
    (clonebranch)
    _arguments \
      {-f,--force}'[Proceeds even if something looks wrong, e.g. the branch_permissions file appears to be missing. (Wit...]' \
      {-n,--name}'[Adds a name prior to the <change#>.<iteration> that describes this particular branch. For example, i...]' \
      {-R,--replace}'[Discards the current client'\''s existing view and maps the new branch to the root of the current clien...]' \
      '*:file:_files'

    ;;
    (commit)
    _arguments \
      {-d,--desc}'[<description> Specifies a description for the new commit.]' \
      '*:file:_files'

    ;;
    (commitlog)
    _arguments \
      -c'[<change#> Specifies the changelist to list commits for. The -c can be omitted if the change# is the ...]':changelist:_g4_describe_cls \
      --files'[Print the files included in each commit.]' \
      --nodeltas'[Omit file deltas.]' \
      --notopo'[By default, only the commits leading to the current one are printed. Specifying this flag will print...]' \
      '*:file:_files'

    ;;
    (copy)
    _arguments \
      '*:file:_files'

    ;;
    (counters)
    _arguments \
      '*:file:_files'

    ;;
    (delete)
    _arguments \
      -c'[<change#> Specifies a changelist in which to delete the files.]':changelist:_g4_describe_cls \
      -k'[Prevents changes from being made to local files but updates the client'\''s state on the server as usua...]' \
      -n'[Turns on display-only mode. In display-only mode no changes are made to any file or changelist, but ...]' \
      --openers'[Prints other places where files being opened are already open.]' \
      '*:file:_files'

    ;;
    (deps)
    _arguments \
      -c'[<change#> The submitted change at which to perform the query. If omitted, the query is performed at ...]':changelist:_g4_describe_cls \
      '*:file:_files'

    ;;
    (describe)
    _arguments \
      -d'[<flags> Passes <flags> on to the diff program.]' \
      -s'[When invoked as describe, omits the diffs from the description. As list, omits the description, outp...]' \
      -w'[Waits until a changelist with at least number <change#> is submitted (or the largest <change#>, if m...]' \
      --follow_ncl'[Returns the full NCL if the specified changelist is submitted (an OCL). Without this flag, describin...]' \
      '*:file:_files'

    ;;
    (diff)
    _arguments \
      -c'[<change#> Displays the differences introduced by the specified changelist. The changelist can be eit...]':changelist:_g4_describe_cls \
      -d'[<flags> Pass <flags> to the external diff program. If not specified, the default value of <flags> is...]' \
      -f'[Display differences for all synced files, including unopened files.]' \
      -s'[<flag> Output a list of files indicating which files differ from the depot or are missing. <flag> se...]' \
      -t'[Assume that all files are text and perform a full diff for each file. By default, binary files produ...]' \
      --since_commit'[<commitid> Displays a diff between current state of the client and a specified commit created with "...]' \
      '*:file:_files'

    ;;
    (diff2)
    _arguments \
      -b'[<branch> Display the differences between the files paired in <branch>. If one pathspec is supplied, ...]' \
      -d'[<flags> Pass <flags> to the external diff program.]' \
      -q'[Displays a list of files that differ rather than full diff output.]' \
      -t'[Assume that all files are text and perform a full diff for each file. By default, binary files produ...]' \
      -u'[Display differences in unified diff format.]' \
      '*:file:_files'

    ;;
    (diffstats)
    _arguments \
      -c'[<change#> Counts only diffs belonging to the specified changelist. If -c is not specified, displays ...]':changelist:_g4_describe_cls \
      -l'[Displays a table with line counts for each file, in addition to the total counts.]' \
      --since_commit'[<commitid> Displays diff stats for the current client relative to the specified commit, instead of r...]' \
      '*:file:_files'

    ;;
    (dirs)
    _arguments \
      -D'[Extends the output to include directories in which all files have been deleted.]' \
      '*:file:_files'

    ;;
    (edit)
    _arguments \
      -c'[<change#> Specifies a changelist under which to open the files.]':changelist:_g4_describe_cls \
      -k'[Prevents changes from being made to local files but updates the client'\''s state on the server as usua...]' \
      -n'[Turns on display-only mode. In display-only mode no changes are made to any changelist or file, but ...]' \
      -t'[<filetype> Specifies that files be opened as type filetype. Piper applies the new filetype to the de...]' \
      --openers'[Prints other places where files being opened are already open.]' \
      '*:file:_files'

    ;;
    (filelog)
    _arguments \
      -i'[Follows branches. By default filelog displays revisions back to the oldest revision of the file. If ...]' \
      -l'[Displays the full description text for changes.]' \
      -L'[Displays up to 250 characters of each change description.]' \
      -m'[<max> Displays at most <max> history items for each matching pathname.]' \
      -s'[Prevents the display of integrations into other files. That is, excludes lines that begin "<action> ...]' \
      -t'[Displays the time as well as the date for changes.]' \
      '*:file:_files'

    ;;
    (files)
    _arguments \
      -a'[Displays all revisions within the range specified by <pathspec>.]' \
      -l'[Prints permissions (SrcProtect) information for each file listed. Each file will either have LIST pe...]' \
      --as_set'[Interprets the argument paths as a single query and returns results as a set instead of returning re...]' \
      '*:file:_files'

    ;;
    (findreviewers)
    _arguments \
      -c'[<change#> Suggests reviewers for the files in this changelist. The changelist can be either a pendin...]':changelist:_g4_describe_cls \
      -i'[<reviewer1> Include the named reviewer. Can be used multiple times.]' \
      -x'[<reviewer2> Exclude the named reviewer. Can be used multiple times. Useful if a person is on vacatio...]' \
      '*:file:_files'

    ;;
    (fix)
    _arguments \
      -c'[<change#> Restricts the fixes to files in the given pending changelist.]':changelist:_g4_describe_cls \
      --format'[<langs> A comma-separated list of languages to format. The default is "--format build,cc,dart,go,jav...]' \
      -v'[If specified, entire files will be fixed instead of only the modified areas. This can introduce unre...]' \
      '*:file:_files'

    ;;
    (fstat)
    _arguments \
      -m'[<max> Restricts the number of results returned.]' \
      -e'[<change#> Limits the output to files that were in the given changelist.]' \
      -O'[Sets additional output options: -Of  Produce output for all revisions of each file, but do not inclu...]' \
      -R'[Restricts the output to files that match specific criteria: -Rc  Only files that are mapped through ...]' \
      '*:file:_files'

    ;;
    (g4d)
    _arguments \
      -f'[Creates a CitC client named clientname if one doesn'\''t already exist. Has no effect if the client alr...]' \
      -d'[Deletes the current CitC client and goes to <clientroot>/..]' \
      -u'[<user> Looks for clients owned by the given user instead of the current Piper user.]' \
      {--multichange,--nomultichange}'[Creates a client that is either multichange or not multichange, overriding the user default. Require...]' \
      '*:file:_files'

    ;;
    (have)
    _arguments \
      --include_unknown'[Appends a "?" to any unknown revision status. A file has unknown revision status if a sync was start...]' \
      '*:file:_files'

    ;;
    (help)
    _arguments \
      '*:file:_files'

    ;;
    (hipupdate)
    _arguments \
      -c'[<change#> The changelist to operate on.]':changelist:_g4_describe_cls \
      -n'[Report out-of-sync HIP metadata as an error, but don'\''t make any changes.]' \
      -v1'[Use the HIP v1 system.]' \
      '*:file:_files'

    ;;
    (info)
    _arguments \
      '*:file:_files'

    ;;
    (integrate)
    _arguments \
      -b'[<branchname> Triggers branch mode, integrating the source and destination specified in the branch sp...]' \
      -c'[<change#> Opens the destination file(s) in the pending change# instead of the default changelist.]':changelist:_g4_describe_cls \
      -f'[Prevents previous integrates from being considered in calculating which revisions to integrate. By d...]' \
      -i'[Allows integration of unrelated files.]' \
      -n'[Turns on display-only mode. In display-only mode no changes are made to any changelist or file, but ...]' \
      -o'[Displays information about the base file used for merges.]' \
      -r'[Changes the direction of a branch integrate. For example, if a branch spec contains the line "//depo...]' \
      -s'[<filespec> Filters the sources for the integration. Only use -s in branch mode (with -b). Note that ...]' \
      -t'[Sets the destination'\''s type to the source'\''s type. By default the destination file retains its type. ...]' \
      -v'[Prevents any files from branches from being added to the client workspace.]' \
      '*:file:_files'

    ;;
    (integrated)
    _arguments \
      -b'[<branchname> Restricts displayed integrations to those for which branch <branchname> maps the integr...]' \
      -r'[Reverses the roles of source and target when filtering through the branch specified with -b.]' \
      '*:file:_files'

    ;;
    (label)
    _arguments \
      -d'[Deletes the label specification for <labelname>.]' \
      -i'[Creates or updates a label from a label specification supplied via standard input.]' \
      -o'[Prints the specification for <labelname> to stdout.]' \
      -t'[Uses existing label <templatename> as a template. The Options field and the View are copied from the...]' \
      --revision'[Prints a specific revision of the label spec instead of the latest revision.]' \
      '*:file:_files'

    ;;
    (labels)
    _arguments \
      -u'[<username> Restricts results to labels owned by <username>.]' \
      -e'[<namepattern> Restricts results to labels that match <namepattern>. The <namepattern> can include th...]' \
      -m'[<max> Display no more than <max> labels.]' \
      '*:file:_files'

    ;;
    (lint)
    _arguments \
      -c'[Specifies the pending or submitted change number for files to lint.]':changelist:_g4_describe_cls \
      -v'[Displays all errors in the specified files.]' \
      '*:file:_files'

    ;;
    (mail)
    _arguments \
      -b'[<bugs> Specifies the comma-separated <bugs> that this changelist addresses; sets the BUG= changelist...]' \
      -c'[<change#> Specifies the changelist for which checks are executed.]':changelist:_g4_describe_cls \
      {-f,--disable_presubmit}'[Disables execution of METADATA Presubmit checks.]' \
      -m'[<reviewers> Specifies the comma-separated <reviewers> that should review this change before submissi...]' \
      -q'[Suppresses autoresolve execution, the change editor, and interactive prompts for reviewers and bugs....]' \
      -x'[Marks bugs in the BUG= tag as "fixed" in Buganizer after the changelist has been submitted.]' \
      {--autorevert,--noautorevert}'[Enable or disable the automatic revert of unchanged and missing files. Enabled by default unless ove...]' \
      --autosubmit'[Enables automatic submit from Critique.]' \
      --cc'[,-z <emails> Specifies the comma-separated <emails> that will be cc'\''d on the code review thread; set...]' \
      --desc'[<description> Sets the description of the change without invoking the editor. Can be combined with -...]' \
      {--diff,--nodiff}'[Enables or disables the execution of diff. Disabled by default unless overridden by G4MAILDIFF in th...]' \
      --disable_review_notify'[Disables adding METADATA Presubmit '\''auto_reviewers'\'' to the R list and '\''review_notify'\'' users to the C...]' \
      --ignore_presubmit_errors'[Ignores errors and warnings from METADATA Presubmit check execution and continues to mail the change...]' \
      --ignore_presubmit_warnings'[Ignores warnings from METADATA Presubmit check execution and continues to mail the changelist.]' \
      --note'[<note to reviewer> Sets note to the reviewer.]' \
      --noupload'[Skips uploading the changelist to Critique.]' \
      --set_tag'[<KEY=value> Sets an arbitrary tag in the changelist description. Specifying an empty value (or omitt...]' \
      --tap_projects'[<projects> Specifies a comma-separated list of TAP projects for which tests will be executed before ...]' \
      --tap_retry'[Forces the TAP presubmit run to use the same base changelist as the previous run (if available) and ...]' \
      '*:file:_files'

    ;;
    (makelink)
    _arguments \
      '*:file:_files'

    ;;
    (map)
    _arguments \
      -i'[Reads the version map from stdin. The user'\''s editor is not invoked.]' \
      -o'[Writes the version map to stdout. The user'\''s editor is not invoked.]' \
      '*:file:_files'

    ;;
    (mapbranch)
    _arguments \
      {-f,--force}'[Proceeds even if something looks wrong, e.g. the branch_permissions file appears to be missing.]' \
      {-R,--replace}'[Discards the current client'\''s existing view and maps the new branch to the root of the current clien...]' \
      '*:file:_files'

    ;;
    (move)
    _arguments \
      -c'[<change#> Specifies a changelist in which to open the integrated and deleted files.]':changelist:_g4_describe_cls \
      -f'[Reverts any opened destination files before integrating to them.]' \
      '*:file:_files'

    ;;
    (newbranch)
    _arguments \
      {-f,--force}'[Proceeds even if something looks wrong, e.g. the branch_permissions file appears to be missing.]' \
      {-m,--components}'[Enables use of components. Generate a version map for the latest stable components release as of the...]' \
      {-n,--name}'[<prefix> Adds a named prefix to the <change#>.<iteration> that describes this particular branch. For...]' \
      {-R,--replace}'[Discards the current client'\''s existing view and maps the new branch to the root of the current clien...]' \
      --version_map'[Interprets the second argument not as @baseline, but the path to a version map file. This version ma...]' \
      '*:file:_files'

    ;;
    (nothave)
    _arguments \
      -a'[Forces all files to be listed, ignoring both the common file filter, and G4NOTHAVEFILTER.]' \
      --print_filter'[Prints the common file filter.]' \
      '*:file:_files'

    ;;
    (opened)
    _arguments \
      -a'[Displays files opened in all clients.]' \
      -c'[<change#> Displays files opened in changelist <change#>.]':changelist:_g4_describe_cls \
      -C'[<clientname> Displays files opened in client <clientname>.]' \
      -m'[<max> Restricts the number of results returned to <max>.]' \
      --edit'[Runs $P4EDITOR with the files as arguments, instead of listing files. Skips deleted and integrate/br...]' \
      '*:file:_files'

    ;;
    (owners)
    _arguments \
      -c'[<change#> Determine owners for files opened in <change#>.]':changelist:_g4_describe_cls \
      '*:file:_files'

    ;;
    (pack)
    _arguments \
      -a'[Produces diffs for binary files. If not specified, the new version of a binary file is written to th...]' \
      -c'[<change#> Specifies which changelist should be packed. If the specified changelist is in another cli...]':changelist:_g4_describe_cls \
      -N'[Treats missing files as empty. By default, deleted files are merely listed as deleted without any fu...]' \
      -o'[<file> Writes the patch to the given file, rather than to standard output. You can also use "-o -" t...]' \
      '*:file:_files'

    ;;
    (patch)
    _arguments \
      -c'[<source-change#> The original change to patch. See -c in '\''g4 help pack'\''.]':changelist:_g4_describe_cls \
      -r'[Revert conflicting files in the target client instead of skipping them. See -r in '\''g4 help unpack'\''.]' \
      -t'[<destination-change#> The target change to open patched files in. May be a change number, '\''default'\'',...]' \
      '*:file:_files'

    ;;
    (pending)
    _arguments \
      -a'[Shows changes in all clients for the current user.]' \
      -c'[<client> Specifies the Piper clients for which to show pending changes. The -c flag is not compatibl...]':changelist:_g4_describe_cls \
      -l'[Shows results in a long format, with full changelist descriptions and affected files.]' \
      -s'[<style> Specifies the output style that is used for short format. This will override the G4PENDINGST...]' \
      -u'[<user> Shows changes for the specified user.]' \
      '*:file:_files'

    ;;
    (populate)
    _arguments \
      {-f,--force}'[Proceeds even if something looks wrong, e.g. some files cannot be represented on the branch. (Withou...]' \
      {-n,--dryrun}'[Simulates by passing -n to '\''p4 integrate'\''.]' \
      '*:file:_files'

    ;;
    (presubmit)
    _arguments \
      -a'[<action> Specifies the action for which presubmit checks are executed. The default is "submit". See ...]' \
      -c'[<change#> Specifies the changelist for which checks are executed.]':changelist:_g4_describe_cls \
      -n'[Outputs the checks that are configured for the changelist without actually executing them.]' \
      --ignore_presubmit_errors'[Ignores errors and warnings from METADATA Presubmit check execution, and always executes checks conf...]' \
      --ignore_presubmit_warnings'[Ignores warnings from METADATA Presubmit check execution, and executes checks configured for seconda...]' \
      --tap_projects'[<projects> Specifies a comma-separated list of TAP projects for which tests will be executed. This l...]' \
      --tap_retry'[Forces the TAP presubmit run to use the same base changelist as the previous run (if available) and ...]' \
      '*:file:_files'

    ;;
    (print)
    _arguments \
      -a'[Displays all revisions within the range specified by <pathspec>.]' \
      -o'[<outfile> Writes the output to a local file instead of to stdout. Only the first file displayed is w...]' \
      -q'[Suppresses header lines. This is implied when running the '\''cat'\'' alias of print.]' \
      '*:file:_files'

    ;;
    (protect)
    _arguments \
      --show_full_table'[Display the entire contents of the SrcProtect table related to the specified user. Cannot be combine...]' \
      -u'[<username> Display permissions for the specified user. Defaults to the current user.]' \
      '*:file:_files'

    ;;
    (readability)
    _arguments \
      '*:file:_files'

    ;;
    (reopen)
    _arguments \
      -c'[<change#> Moves <file>s from their current changelist to <change#>. Use -c default to move <file>s t...]':changelist:_g4_describe_cls \
      -R'[Reconciles <file>s according to the argument(s) given to -R: include '\''e'\'' to reconcile edited files, ...]' \
      -t'[<filetype> Changes the type of <file>s to <filetype>, effective in the depot when the changelist is ...]' \
      '*:file:_files'

    ;;
    (resolve)
    _arguments \
      -a'[<resolveMode> Resolves all conflicts with mode <resolveMode>. If no <resolveMode> argument is suppli...]' \
      -f'[Forces resolution even if the file has already been resolved.]' \
      -n'[Turns on display-only mode. In display-only mode no changes are made to any file, but output is prin...]' \
      -o'[Outputs information about the base file used for merges.]' \
      -t'[Treat binary files like text files when performing merge.]' \
      -v'[Inserts markers into the resolved file for all changes, not just for conflicts.]' \
      '*:file:_files'

    ;;
    (resolved)
    _arguments \
      -o'[Displays information about the base file used for merges.]' \
      '*:file:_files'

    ;;
    (restore)
    _arguments \
      -k'[Prevents changes from being made to local files but updates the client'\''s state on the server as usua...]' \
      '*:file:_files'

    ;;
    (revert)
    _arguments \
      -a'[Restricts the affected files to unchanged files.]' \
      -c'[<change#> Restricts affected files to those open in <change#>. If no filepaths are specified, then t...]':changelist:_g4_describe_cls \
      -k'[Prevents changes from being made to local files but updates the client'\''s state on the server as usua...]' \
      -n'[Turns on display-only mode. In display-only mode no changes are made to any changelist or file, but ...]' \
      '*:file:_files'

    ;;
    (rollback)
    _arguments \
      -c'[<change#> Specifies the changelist to roll back. The change# may be the last argument without a -c.]':changelist:_g4_describe_cls \
      {-r,--reason}'[<reason> Allows you to specify the reason for the rollback that will be then put into the changelist...]' \
      --use_default_changelist'[Skips the creation of the new pending changelist and opens files for rollback in default changelist ...]' \
      '*:file:_files'

    ;;
    (set)
    _arguments \
      -q'[Outputs only the variable values, suppressing output of variable names and "(config)" markers.]' \
      '*:file:_files'

    ;;
    (sizes)
    _arguments \
      -a'[Displays the sizes of all historical revisions specified by <pathspec> arguments.]' \
      -b'[<blocksize> Displays the size in blocks of <blocksize> bytes in addition to displaying the size in b...]' \
      -h'[Displays the size in human-readable format (e.g., 1K, 2.3M).]' \
      -s'[Displays a sum of revision counts and sizes for all revisions specified by <pathspec> arguments.]' \
      '*:file:_files'

    ;;
    (speclog)
    _arguments \
      -m'[<max> Restricts the number of results returned.]' \
      '*:file:_files'

    ;;
    (submit)
    _arguments \
      -b'[<bugs> Specifies the comma-separated <bugs> that this changelist addresses; sets the BUG= changelist...]' \
      -c'[<change#> Indicates the number of an existing pending changelist to be submitted. If -c is provided,...]':changelist:_g4_describe_cls \
      {-d,--desc}'[<description> Specifies a description for the new pending changelist. The changelist will be created...]' \
      {-f,--disable_presubmit}'[Disables execution of METADATA Presubmit checks. If your goal is to only disable test execution, add...]' \
      -i'[Creates or updates a changelist from a change specification supplied via standard input. See "p4 hel...]' \
      -m'[<reviewers> Specifies the comma-separated <reviewers> that should review this change before submissi...]' \
      -n'[Tests submitting a change without actually submitting it.]' \
      -u'[<username> Tests submitting a change as a user other than the user running the command. Requires -n....]' \
      -x'[Marks bugs in the BUG= tag as "fixed" in Buganizer after the changelist has been submitted.]' \
      --ignore_presubmit_errors'[Ignores errors and warnings from METADATA Presubmit check execution and continues to submit the chan...]' \
      --ignore_presubmit_warnings'[Ignores warnings from METADATA Presubmit check execution and continues to submit the changelist.]' \
      --set_tag'[<KEY=value> Sets an arbitrary tag in the changelist description. Specifying an empty value (or omitt...]' \
      --tap_projects'[<projects> Specifies a comma-separated list of TAP projects for which tests will be executed before ...]' \
      --tap_retry'[Forces the TAP presubmit run to use the same base changelist as the previous run (if available) and ...]' \
      --tbr'[<reviewers> Specifies the comma-separated <reviewers> that should promptly review this change after ...]' \
      '*:file:_files'

    ;;
    (switch)
    _arguments \
      -s'[The name of the switching client; this will be implicitly created if it does not exist yet. If this ...]' \
      '*:file:_files'

    ;;
    (switches)
    _arguments \
      '*:file:_files'

    ;;
    (sync)
    _arguments \
      -f'[Forces the depot versions of all files in the client view to overwrite the client versions even if t...]' \
      -k'[Prevents changes from being made to local files but updates the client'\''s state on the server as usua...]' \
      -n'[Turns on display-only mode. In display-only mode no changes are made to any file, but output is prin...]' \
      -o'[If a sync is ambiguous (the same file is requested at different revisions), report an error instead ...]' \
      -p'[Downloads files to the client without modifying client state on the server. This means that the sync...]' \
      -q'[Suppresses regular output. Errors and warnings are still printed. When used with -k, this may signif...]' \
      '*:file:_files'

    ;;
    (tabcomplete)
    _arguments \
      --token'[Complete the Nth (counting from one) token in the command line. (required)]' \
      '*:file:_files'

    ;;
    (test)
    _arguments \
      '*:file:_files'

    ;;
    (typemap)
    _arguments \
      '*:file:_files'

    ;;
    (unpack)
    _arguments \
      -r'[Reverts any opened files that are present in the packfile, instead of skipping them. Use with cautio...]' \
      -t'[<change#> Designates the target changelist in which the modified files are opened. Possible values a...]' \
      '*:file:_files'

    ;;
    (upload)
    _arguments \
      -c'[The affected changelist]':changelist:_g4_describe_cls \
      '*:file:_files'

    ;;
    (user)
    _arguments \
      -d'[Deletes the user specification for <username>.]' \
      -i'[Creates or updates a user from a user specification supplied via standard input.]' \
      -o'[Prints the specification for <username> to stdout.]' \
      --revision'[<revision#> Prints a specific revision of the label spec instead of the latest revision.]' \
      '*:file:_files'

    ;;
    (users)
    _arguments \
      -m'[<max> Restricts the displayed list to the first <max> users returned.]' \
      '*:file:_files'

    ;;
    (where)
    _arguments \
      '*:file:_files'

    ;;
    (whitelist)
    _arguments \
      --access_group'[<group> By default, users are registered to piper-group-p4roleusers-srcfs-nonconf for read access. A...]' \
      --desc'[<description> A short description of the reason for the change, ideally including buganizer ids and/...]' \
      --dry_run'[Optional. If set, only validation checks are performed, and the process stops just short of actually...]' \
      --register'[Instruction to grant the Ganpati group read-only access to Piper.]' \
      -u'[<groupname> Ganpati group to grant additional permissions to. Required.]' \
      '*:file:_files'

    ;;
  esac
}
