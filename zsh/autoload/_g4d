#compdef g4d

_g4d() {
  if [[ ! -d /google/src/cloud ]]
  then
    >&2 echo "Please run gcert."
    return
  fi

  local citcclients=($(ls /google/src/cloud/$USER))
  _alternative "args:citc:($citcclients)"
}

_g4d
